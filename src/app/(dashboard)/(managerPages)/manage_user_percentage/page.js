import React from 'react'

const page = () => {
  return (
    <div>
      After adding a User, the Manager must set commission percentages for that User on each product. The Manager can
      assign the same percentage to all products at once for a User. Later, the Manager can update individual product
      percentages separately if needed by selecting single products.
    </div>
  )
}

export default page
